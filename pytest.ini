[pytest]
testpaths = tests
addopts =
    -v
    --browser chromium
    --screenshot only-on-failure
    --video retain-on-failure
    --tracing retain-on-failure
markers =
    smoke: critical tests covering core functionality
    regression: broader functional regression tests

log_cli = true
log_cli_level = INFO